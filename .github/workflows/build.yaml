name: build and push images
on:
  push:
    paths:
    - "k8s/**"
    #branches:
    #  - main
    #tags:
    #  - 'v*.*.*'
jobs:
  docker-build-and-publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@main
      - run: |
            ls -la
